1.在牵扯到金钱的计算时（要求精度很高的情况），使用bigDecimal而不用double
 
2.ArrayList集合类就只能以类作为他的存储对象，而这时如果想把一个int型的数据存入list是不可能的，必须把它包装成类
3.泛型只能使用引用类型，而不能使用基本类型
4.基本类型和引用类型不能直接进行转换，必须使用包装类来实现
 
5.Java的Integer类内部实现了一个静态常量池，用于储存特定范围内的整数值对应的Integer对象，默认情况下，这个范围是-128至127。
 
6.接口中不能有构造函数
	接口中可以定义，默认方法（default）、静态方法、私有方法、抽象方法
	 
7.反射：Java 反射机制是在运行状态中，对于任意一个类，都能够知道这个类中的所有属性和方法，对于任意一个对象，都能够调用它的任意一个方法和属性；这种动态获取的信息以及动态调用对象的方法的功能称为Java语言的反射机制。

Object中的方法：
8.equals方法默认比较的对象的地址值，如果要比较对象的内容那么就要对equals方法进行重写（注意：配套的也要去重写hashCode方法，因为在equals方法中定义两个对象相等的话那么hashCode也要相等）

9.getClass方法：返回对象运行时的实际类对象，常用于反射场景中，去通过getClass获取类的方法和属性

10.String、StringBuffer、StringBuilder（性能一次递增）
	1.String不可变后两者都可变
	2.线程安全：String安全（不可变）、StringBuffer安全（内部加了synchronized锁）、StringBuilder线程不安全
	
11.Stream：提供一组高效且易于使用的数据处理方式，特别适合集合对象的操作例如：过滤、映射排序，提高了代码的简洁性和可读性还是利用多核处理的优势进行并行处理
Stream流：串行流和 并行流（将一个任务拆分为多个小任务，最后再将小任务结果合并计算结果）
